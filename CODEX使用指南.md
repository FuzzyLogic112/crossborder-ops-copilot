# 在 Codex 里用这套 Skills

**为什么用 Codex 跑这部分**：它自带生图模型，产品图的「写提示词 → 出图 → 自查 → 改」
可以在一个对话里闭环，不用在两个工具之间来回倒。

其余三个 skill（选品 / 跟价 / 文案）在 Codex 和 Claude 里都能跑，
哪个顺手用哪个。**额度紧张时优先把生图放 Codex**，因为那是它独有的能力。

---

## 一、最快上手：三步

### 1. 把 skill 内容粘进去

Codex 没有 Claude Code 那样的 skill 自动加载机制，**直接粘全文**即可：

```
下面是我的选品判断 SOP，请严格按它执行，不要用你自己的方法：

（粘贴 skills/product-screening/SKILL.md 全文）

现在按这个流程分析这个品：
（粘贴你的 JSON 输入）
```

**关键是「严格按它执行，不要用你自己的方法」这句。** 不加这句，
模型容易滑回泛泛而谈的通用建议，那样 skill 就白粘了。

### 2. 给真实数据

数据从面板里拿：

| 要什么 | 去哪拿 |
|---|---|
| 竞品售价、评分、评价数、排名 | 面板「② 竞品数据」表格 |
| 重量、件数、中文品类 | 面板「③ 选品工作台」的「规格」列 |
| 保本价、贡献利润、保本 ROAS | 面板「⑥ 定价计算器」 |
| 到仓成本 | 面板「④ 供应商比价」 |

**别让 AI 猜这些数。** 它会编一个看起来合理的值，而你分不出来。

### 3. 按输出格式要求它

每个 SKILL.md 末尾都有「输出格式」。明确要求：

```
输出必须严格按 SKILL.md 里的「输出格式」小节，包括「不确定的地方」那一节 ——
拿不准的必须列出来，不要假装什么都清楚。
```

**最后那句很重要。** 不加的话模型倾向于给一个自信满满的完整答案，
而选品里有大量它根本无从判断的东西（比如你的供应链能力、你的资金周转）。

---

## 二、生图工作流（Codex 的主场）

### 完整对话示例

**第 1 轮 —— 生成提示词**

```
下面是我的产品图 SOP：

（粘贴 skills/listing-visuals/SKILL.md 全文）

产品信息：
{
  "product_name": "不锈钢真空保温杯",
  "category": "kitchen",
  "key_features": ["40oz 大容量", "手柄便携", "24 小时保温"],
  "target_market": "US",
  "image_type": "main",
  "brand_tone": "简洁实用",
  "notes": "杯身哑光黑，有提手和吸管"
}

按 SOP 生成主图提示词，必须带 negative 提示。
```

**第 2 轮 —— 出图**

```
用上面的提示词生成图片。
```

**第 3 轮 —— 自查**

```
按 SKILL.md 里的「出图后自查清单」逐条检查刚才这张图，
不合格的项说清楚具体哪里不合格、怎么改提示词。
不要说"基本符合"，逐条给结论。
```

**第 4 轮 —— 迭代**

```
按你指出的问题改提示词，重新生成。
```

### 生图时最容易踩的三个坑

**① 模型爱自作主张加文字。**
主图有文字在亚马逊是违规的。**negative 提示里必须写 `text, watermark, logo, border`**，
而且出图后要自己看一眼 —— negative 不是百分百生效。

**② 背景不是纯白。**
模型经常给"接近白"的浅灰（比如 250,250,250）。肉眼看不出，但平台机审会挑出来。
**用取色器点图片四角确认是 255,255,255**，不是就让它重画或后期处理。

**③ 让模型写字必错。**
功能图、尺寸图上的说明文字、箭头、数字 —— **一律后期加**。
生图模型写出来的英文几乎必有拼写错误或乱码，错别字出现在 Listing 图上非常伤转化。
所以模板里都留了空白区域给你后期放字。

### 一次生成一整套

```
按 SOP 依次给我这个产品的五种图提示词：main / scene / feature / size / aplus，
每种都带 negative 提示。先只给提示词，不要出图，我确认后再逐张生成。
```

**先确认提示词再出图**，比出完一堆再返工省得多。

---

## 三、四个 skill 的典型用法

### 选品初筛

```
（粘贴 product-screening/SKILL.md）

严格按这个 SOP 判断，先过四道硬性淘汰再打分。输入：
{
  "product_name": "...",
  "reference_price_local": 31.99, "currency": "USD", "fx_rate": 7.1,
  "weight_kg": 0.55, "cost_price_cny": 29.0, "packaging_cost_cny": 3.5,
  "platform": "amazon_fba", "market": "US",
  "competitor_rank": 3, "competitor_reviews": 17450, "competitor_rating": 4.6,
  "negative_review_themes": ["吸管漏水", "提手容易断"]
}
```

**重量一定要填真实值。** 面板「③ 选品工作台」的「规格」列会从标题里解析出来
（`Cat Litter 40 lb` → 18.1kg）。填错重量，整个结论都是错的。

### 跟价决策

```
（粘贴 price-response/SKILL.md）

竞品降价了，按 SOP 给我应对建议。注意：降价是最后手段，
先看有没有非价格的办法。输入：
{ ... }
```

### Listing 文案

三层要**分三轮**跑，不要一次全给：

```
第 1 轮：（粘贴 planning_layer.md）+ 产品信息  →  得到策略卡 JSON
第 2 轮：（粘贴 execution_amazon.md）+ 上一轮的策略卡  →  得到亚马逊文案
第 3 轮：（粘贴 qa_layer.md）+ 上一轮的文案  →  得到合规风险提示
```

**一次全给会得到"好看但不统一"的文案** —— 三个平台各说一套卖点。
决策层的作用就是先把卖点排序定死。

---

## 四、省额度的用法

Codex 额度有限，几条实用的：

| 做法 | 省在哪 |
|---|---|
| **先确认提示词，再出图** | 出图比出文字贵得多，返工代价高 |
| **一次问一个品** | 批量分析的输出长、容易泛泛而谈，反而要重来 |
| **能用面板算的不要问 AI** | 保本价、贡献利润、到仓成本面板直接出，问 AI 还可能算错 |
| **选品/跟价放 Claude，生图放 Codex** | 生图是 Codex 独有能力，把额度花在别处可惜 |
| **让它直接给结论，别让它"分析一下"** | 明确要求按输出格式，避免大段铺垫 |

**最重要的一条**：**面板能算的数不要交给 AI 算。**
保本价是 `固定成本 ÷ (1 − 费率)`，AI 有不小概率写成乘法。
面板里这个公式有 JS 和 Python 两套实现互相自校验，算错会当场报错。

---

## 五、Codex 做不了的事

- **不能抓竞品数据。** 抓取要跑 Playwright，用面板「① 抓取数据」。
- **不能读你的 products.csv。** 数据要你手动粘进对话。
- **不能改价上架。** 这个项目不提供任何写操作接口，平台动作必须人工执行。
- **生成的图不能直接当主图用。** AI 图适合做方案验证、A+ 背景、广告素材草稿；
  **主图强烈建议真实拍摄** —— 图文不符带来的退货和投诉，比省下的拍摄费贵得多。

---

## 六、一个完整的上新流程（把四个 skill 串起来）

```
① 面板抓竞品           → 拿到榜单数据、重量、中文品类
② Codex 跑选品初筛      → 判断能不能做（先过四道硬淘汰）
③ 去 1688 问 3 家报价   → 人工，AI 替不了
④ 面板跑供应商比价      → 得到真实到仓成本
⑤ 面板跑定价计算器      → 保本价、贡献利润、保本 ROAS
⑥ Codex 跑 Listing 三层 → 文案（分三轮）
⑦ Codex 跑产品图        → 提示词 → 出图 → 自查 → 迭代
⑧ 人工上架              → 平台后台操作
⑨ 次日起面板跑变化监控   → 竞品动了再用跟价决策 skill
```

**第 ③ 步和第 ⑧ 步是 AI 完全替不了的**，其余都能提速。

**别指望第一次就跑顺。** 先拿一个便宜、轻、不涉及认证的品走一遍，
把流程跑通比第一个品赚钱重要。
