# 真实数据全流程跑通指南

**这份指南里的每一步、每个数字，都是 2026-09-12 当天实跑出来的真实输出**，不是设想的。
实跑案例：美国站 **家居厨房**类目（`kitchen`），全程 68 条真实竞品数据。

> **面板 Tab 的编号就是流程顺序**：①→⑧ 依次点下来就是一遍完整运营动作。
> 全程约 30–40 分钟，其中只有第 ④ 步需要你离开工具去问供应商报价。

---

## 流程总览

```
①抓取数据 ──→ ②竞品数据 ──→ ③选品工作台 ──→ ④供应商比价
  抓竞品        看变化日报      挑候选+填字段      归一化到仓成本
                                                        │
                                                        ▼
⑧变化监控 ←── ⑦文案生成 ←── ⑥定价计算器 ←── ⑤选品评分
  次日复跑       出 Prompt      核算贡献利润      六维打分+硬淘汰

配置 / 数据存储 / 说明 —— 不在主流程里，需要时才进
```

**每一步的产出就是下一步的输入**，所以顺序不能跳：
没抓数据就没有候选品，没到仓成本就算不出利润，没利润就评不了分。

---

## 开始前

```bash
python serve.py --open
```

浏览器自动打开 `http://127.0.0.1:8911`，顶部应显示 **「完整模式 —— 本地服务已连接」**，
默认落在 **「① 抓取数据」**。

看到「只读演示模式」就是服务没连上 —— 看终端有没有报错，或换个端口（见文末报错表）。

---

# ① 抓取数据

**Tab →「① 抓取数据」**

| 填什么 | 我实跑填的值 | 说明 |
|---|---|---|
| 站点 | `us ✓` | 带 ✓ 的是实测验证过的 |
| 类目 slug | `kitchen` | **自由填**，不限于内置列表 |
| 页数（上限 3） | `2` | **记住这个数，第⑧步对比必须一致** |
| 间隔秒数（下限 3） | `3` | 服务端强制下限，改小无效 |

点 **开始抓取**。任务日志区的真实输出：

```
[09:11:27] 启动抓取：us / kitchen / 2 页 / 间隔 3.0s
[09:11:42] 抓取完成，开始转换为标准快照格式
[09:11:42] 已转换 68 条 -> competitors_us_kitchen_2026-09-12.csv
[09:11:42] 已入库 68 条
```

**15 秒，68 条真实数据。**

**✅ 算成功**：日志出现「已入库 N 条」，且 N > 0。

抓到的真实数据（前 8 条，未经任何加工）：

| 排名 | ASIN | 标题 | 价格 USD | 评分 | 评价数 |
|---|---|---|---|---|---|
| 1 | B0BZYCJK89 | Owala FreeSip Stainless Steel Water Bottle | 29.99 | 4.6 | 137445 |
| 2 | B0CP9YB3Q4 | STANLEY Quencher H2.0 Tumbler with Handle | 31.95 | 4.7 | 204901 |
| 3 | B0CQVWT2NH | HydroJug Traveler 40oz, Insulated Tumbler | 31.99 | 4.6 | 17450 |
| 4 | B0113UZJE2 | Etekcity Food Kitchen Scale, Digital | 10.99 | 4.6 | 177064 |
| 5 | B0DF3M7V3T | Owala SmoothSip Slider Insulated | 23.99 | 4.6 | 14667 |
| 6 | B0DR9PMK1W | STANLEY IceFlow 2.0 Flip Straw Tumbler | 21.00 | 4.7 | 69375 |
| 7 | B0CYJBB2JQ | bella Fits-Anywhere 2-Slice Slim Toaster | 24.98 | 4.2 | 20340 |
| 8 | B06X9NQ8GX | Amazon Basics Digital Kitchen Scale | 9.89 | 4.7 | 118657 |

### 类目 slug 怎么找

**类目不需要配置，本来就是自由填的** —— 只校验格式（小写字母/数字/连字符），不限取值。
因为各站 slug 不同：**美国站玩具是 `toys-and-games` 而不是 `toys`**。

抓不到时：打开目标站榜单页，看地址栏 `/gp/bestsellers/<这一段>/` 的真实值。

已实测可用（US）：`kitchen`、`pet-supplies`、`toys-and-games`、`office-products`

### 这一步做不到什么

- **只抓 Amazon 榜单页**。1688 / Alibaba / 义乌购 / DHgate 的价格全在登录墙或反爬后面
  （实测 1688 正文仅返回 97 字符、AliExpress 撞 Sign in 墙），**项目边界规定遇登录墙即停**。
  所以供应商报价只能人工录入 —— 见第④步。
- 页数上限 3、间隔下限 3 秒是**服务端强制**的，不是建议值。

---

# ② 竞品数据

**Tab →「② 竞品数据」**

这一页不用填任何东西，它把刚抓到的快照渲染成两块：

1. **竞品变化日报** —— 降价 / 涨价 / 新品上榜 / 下架 / 促销变化 / 评分变化，每类配一条处理建议
2. **竞品快照表** —— 刚抓到的全部行

**第一次抓某个类目时，日报一定是空的** —— 只有一天数据，没有可比的前一天。
这是正常的，不是 bug。日报要到第⑧步、明天复跑之后才有内容。

> **日报的设计原则**：降价的默认建议是「先核算己方贡献利润再决定是否跟价」，
> **不是直接跟价** —— 跟价可能把自己打到亏损线以下。这也是第⑥步存在的理由。

---

# ③ 选品工作台

**Tab →「③ 选品工作台」** —— 这是「抓到的竞品」变成「可评分的候选品」的地方。

## 第 ① 小节：从真实竞品里挑候选品

| 填什么 | 我实跑填的值 |
|---|---|
| 选一份竞品快照 | `data/real/competitors_us_kitchen_2026-09-12.csv`（68 行） |
| 归到哪个类目（影响退货率） | `kitchen` |
| 平台所在市场 | `US` |
| 汇率（当地货币 → 人民币） | `7.1` |

点 **加载快照** → 勾选要做的行 → 点 **把勾选的加入候选**。

我勾了两个（故意一高价一低价，好看出差异），真实返回：

```
SKU        : B0CQVWT2NH
名称       : HydroJug Traveler 40oz, Insulated Tumbler
当地售价   : 31.99 USD
计划售价   : ¥227.13  （= 31.99 × 7.1）
参考       : 评分 4.6 / 评价数 17450 / 排名 3

SKU        : B0113UZJE2
名称       : Etekcity Food Kitchen Scale, Digital
当地售价   : 10.99 USD
计划售价   : ¥78.03   （= 10.99 × 7.1）
参考       : 评分 4.6 / 评价数 177064 / 排名 4
```

### ⚠️ 汇率这一栏不能忽略

成本模型**全部以人民币计价**（采购价来自 1688，物流费率也是人民币报价），
但抓来的售价是**当地货币**。两者直接相减会得出完全错误的结论。

这是实际踩过的坑：不换算时，三个真实商品全被判成「亏损淘汰」——
因为拿 `22.99`（美元）去减 `¥29`（人民币成本）。

**默认汇率 7.1 是占位量级，不是实时汇率**，用前请自己核实当日汇率。

## 第 ② 小节：补齐抓不到的字段

抓取能给你的只有：SKU、名称、计划售价（= 竞品售价×汇率）、评分/评价数/排名参考。

**下面这些抓不到，必须你自己填**：

| 字段 | 从哪来 |
|---|---|
| `cost_price` 采购价 | **先留空**，第④步比价后自动写回 |
| `packaging_cost` 包装成本 | 问供应商，或按类目经验估 |
| `weight_kg` 计费重量 | 问供应商，或按同类产品估 |
| `demand_score` 需求分 | 你看榜单排名/评价数自己判断（满分 25） |
| `gap_score` 竞争缺口分 | 看评论差评里的未满足需求（满分 20） |
| `logistics_score` 物流友好分 | 看重量/体积/是否带电带磁（满分 15） |
| `content_score` 内容可做性分 | 看能不能拍出差异化素材（满分 10） |

填完点 **保存到 products.csv 并重新评分**。

> 想快速试跑可以点 **填入示例数值** —— 但那是占位数，**不能当结论用**。

**✅ 算成功**：第③小节出现评分结果，且你填的商品在列表里。

---

# ④ 供应商比价

**Tab →「④ 供应商比价」** —— 整个工具里最值钱的一步。

## 为什么不能直接比标价

标价最低的，到仓成本经常**不是**最低的。因为标价之外还有：
阶梯价、MOQ、含不含税、含不含运费、样品费/模具费的摊销。

## 第 ① 小节：录入各平台报价

| 填什么 | 我实跑填的值 |
|---|---|
| 给哪个候选品比价 | `B0CQVWT2NH` |
| 你的采购量（件） | `200` |
| 需要增值税专用发票吗 | **需要**（出口退税通常需要） |
| 增值税率 | `0.13` |

点 **＋ 加一条报价** 录入三家（这是真实询价常见的结构）：

| 平台 | 供应商 | 标价 | MOQ | 含税 | 含运 | 国内运费 | 样品费 | 模具费 | 交期 |
|---|---|---|---|---|---|---|---|---|---|
| 1688 | 义乌保温杯厂A | 26.50 | 100 | ✗ | ✗ | 320 | 60 | 0 | 7天 |
| 1688 | 永康工厂B | **23.80** | 500 | ✗ | ✗ | 450 | 80 | 1200 | 15天 |
| 义乌购 | 档口C | 29.00 | 50 | ✓ | ✓ | 0 | 0 | 0 | 3天 |

点 **比价**。

## 第 ② 小节：到仓成本对比（真实输出）

```
平台      供应商          标价   MOQ    含税价  运费/件  摊一次性   到仓单价
--------------------------------------------------------------------------
义乌购    档口C         29.00    50   29.00    0.00     0.00     29.00
1688     永康工厂B      23.80   500   26.89    0.90     2.56     30.35
1688     义乌保温杯厂A   26.50   100   29.95    1.60     0.30     31.84
```

**结论（系统自动判定，原文）**：

> **标价陷阱**：1688 的永康工厂B标价最低（**¥23.80/件**），但到仓成本 **¥30.35** 反而更高；
> 义乌购的档口C标价 ¥29.00、到仓 ¥29.00 才是更优选择。**直接比标价会选错。**
>
> 胜出：义乌购 / 档口C，到仓 ¥29.00，领先第二名 ¥1.354（4.67%）
>
> ⚠️ 1 个报价的 MOQ 高于你的采购量，选它们意味着压货

每条报价还会带自己的警告，实跑输出的原文：

```
永康工厂B：
  ⚠ MOQ 500 高于你的采购量 200 —— 实际要买 500 件，多出的 300 件是压货风险
  ⚠ 报价不含税，按需要专票计入 13% 增值税
  ⚠ 没有报价日期 —— 报价会变，无日期的报价不能作为决策依据
```

> **注意 MOQ 不达标的报价没有被自动剔除** —— 这是故意的。
> 压 300 件货换单价低 ¥2，值不值得是**商业判断，该留给人做**，工具只把代价算清楚摆出来。

## 第 ③ 小节：用选中的报价作为采购价

选 `义乌购 / 档口C` → 到仓成本单价自动填 `29.00` → 点 **写入采购价并重新评分**。

真实返回：

```json
{
  "sku": "B0CQVWT2NH",
  "cost_price": 29.0,
  "backup": "products.backup-20260912-091350.csv",
  "note": "已写入采购价。到「选品工作台」第③步或「选品评分」页看重算后的结果。"
}
```

**✅ 算成功**：返回里有 `backup` 文件名（说明改动前已自动备份）。

### ⚠️ 这一步的诚实边界

**报价必须人工录入，工具不抓供货平台。** 原因见第①步末尾。
所以这一步的价值不在「帮你找便宜货」，而在**把你已经拿到的几份报价算成可比的同一个口径**。

---

# ⑤ 选品评分

**Tab →「⑤ 选品评分」**

| 填什么 | 我实跑填的值 |
|---|---|
| 平台 | `amazon_fba` |
| 目标市场 | `US` |
| 场景 | `标准 30%` |

真实评分结果（★ 是这次新加的两个真实商品）：

| | SKU | 名称 | 总分 | 利润分 | 结论 |
|---|---|---|---|---|---|
| | TRAVEL-001 | 旅行折叠收纳袋 | 83.6 | 14.58 | 通过 |
| ★ | **B0CQVWT2NH** | HydroJug Traveler 40oz | **83.0** | **20.0** | 通过 |
| | TRAVEL-003 | 宠物伸缩牵引绳 | 74.2 | 9.17 | 通过 |
| | TRAVEL-002 | 真空压缩收纳袋 | 71.0 | 10.95 | 通过 |
| ★ | **B0113UZJE2** | Etekcity Kitchen Scale | **70.47** | **9.47** | 通过 |
| | TRAVEL-009 | 防水手机袋 | 70.0 | 10.0 | 通过 |
| | TRAVEL-005 | 磁吸车载手机支架 | 67.2 | 11.21 | 通过 |
| | TRAVEL-007 | 桌面收纳盒 | 66.1 | 11.11 | 通过 |
| | TRAVEL-004 | 儿童安全座椅头枕 | 56.8 | 10.8 | 通过 |
| | TRAVEL-006 | 美白牙贴 | **0.0** | 0.0 | **淘汰** |
| | TRAVEL-008 | 户外折叠椅 | **0.0** | 0.0 | **淘汰** |
| | TRAVEL-010 | 儿童退热贴 | **0.0** | 0.0 | **淘汰** |

## 两种硬性淘汰，真实原文

**淘汰一：赚不到钱**

```json
{
  "sku": "TRAVEL-008", "name": "户外折叠椅", "total_score": 0.0,
  "demand_score": 17.0, "gap_score": 14.0, "profit_score": 0.0,
  "logistics_score": 7.0, "compliance_score": 10.0, "content_score": 8.0,
  "eliminated": true,
  "eliminate_reason": "贡献利润为负或为零，成本无法覆盖，直接淘汰"
}
```

注意它**需求 17、缺口 14、内容 8 都不差**，合规也是满分 10 —— 单项加起来 56 分。
但贡献利润 ≤ 0，**总分直接归零**。这就是「硬性淘汰」的意思：
不是扣分，是一票否决。**需求好但赚不到钱的品，不是好品。**

**淘汰二：合规红线**

```json
{
  "sku": "TRAVEL-006", "name": "美白牙贴", "total_score": 0.0,
  "demand_score": 24.0, "gap_score": 18.0, "compliance_score": 0.0,
  "eliminated": true,
  "eliminate_reason": "合规标记为 banned：涉及禁限售或功效宣称风险，直接淘汰"
}
```

需求 24（满分 25）、缺口 18（满分 20）—— **全场最高的两项**，照样归零。

## 六项里只有一项是系统算的

| 维度 | 满分 | 谁给的 |
|---|---|---|
| 需求 demand | 25 | **你填** |
| 竞争缺口 gap | 20 | **你填** |
| **利润 profit** | **20** | **系统算**（唯一） |
| 物流友好 logistics | 15 | **你填** |
| 合规 compliance | 10 | 你填标记，系统映射分值 |
| 内容可做性 content | 10 | **你填** |

**这一点面试必须讲清楚**：这不是「AI 帮我选品」，是
**「我把判断标准结构化了，让利润这一项不可能拍脑袋」**。
利润分 = `min(20, 贡献利润率 / 目标毛利率 × 20)`，算错了立刻能被查出来。

---

# ⑥ 定价计算器

**Tab →「⑥ 定价计算器」**

用第④步得到的真实到仓成本 ¥29.00、第③步得到的计划售价 ¥227.13 核算：

| 填什么 | 值 |
|---|---|
| 采购价（元） | `29.00` ← 来自第④步 |
| 包装成本（元） | `3.5` |
| 计费重量（kg） | `0.55` |
| 类目 | `kitchen` |
| 平台 | `amazon_fba` |
| 目标市场 | `US` |
| 毛利场景 | `标准 30%` |
| 实际售价（元） | `227.13` ← 来自第③步（竞品定价） |

## 真实输出 A：按竞品实际售价

```json
{
  "price": 227.13,
  "cost_price": 29.0,
  "packaging_cost": 3.5,
  "logistics_cost": 26.0,
  "commission_fee": 34.07,
  "payment_fee": 0.0,
  "return_reserve": 18.17,
  "contribution_profit": 116.39,
  "contribution_margin": 0.5124,
  "breakeven_price": 75.97,
  "breakeven_roas": 1.95
}
```

## 真实输出 B：售价留空（按 30% 目标毛利反推）

```
price                  124.47
breakeven_price         75.97
contribution_profit     37.34
contribution_margin     0.30
breakeven_roas          3.33
```

## 这两组数并排看，才是这一步的价值

| | 按竞品定价 ¥227.13 | 按 30% 毛利反推 ¥124.47 |
|---|---|---|
| 贡献利润 | **¥116.39** | ¥37.34 |
| 贡献利润率 | **51.24%** | 30.00% |
| 保本 ROAS | **1.95** | 3.33 |

**结论**：竞品卖 ¥227，而我只要卖到 ¥124 就有 30% 毛利 —— 中间有 **¥102 的定价空间**。
保本 ROAS 从 3.33 降到 1.95，意味着**投放容错率高一倍多**。

保本价 ¥75.97 是底线：**跌破它，卖一件亏一件**。第②步日报说「降价先别急着跟」，
要跟的就是这个数。

## 为什么保本价是「除以 (1 − 费率)」

```
保本价 = 固定成本 / (1 - 费率合计)
```

**不是乘。** 佣金、支付费、退货准备金都是**按售价的百分比**收的 ——
售价越高，平台抽走的绝对额越大。用乘法会系统性低估保本价，越贵的品错得越多。

```
本例：固定成本 = 29.00 + 3.50 + 26.00 = 58.50
      费率合计 = 15%(佣金) + 0%(支付) + 8%(退货) = 23%
      保本价   = 58.50 / (1 - 0.23) = 75.97  ✓ 与系统输出一致
```

> 面板里这个公式有一份 **JS 实现**，启动时会拿它和 Python 的结果做自校验 ——
> 两边算出来不一样就会在页面上报错。

**✅ 算成功**：页面底部的自校验没有报错。

---

# ⑦ 文案生成

**Tab →「⑦ 文案生成」** —— 三层 Prompt 架构

| 填什么 | 我实跑填的值 |
|---|---|
| 产品名称 | `HydroJug Traveler 40oz 保温杯` |
| 类目 | `kitchen` |
| 核心卖点（一行一条） | `40oz 大容量` / `手柄便携` / `不锈钢真空保温` |
| 目标市场 | `US` |
| 语气基调 | `实用理性` |
| 生成哪一层 | `① 决策层 · 内容策略卡` |

点 **生成 Prompt** → 状态栏显示「已用服务端真实模板生成（**1052 字符**）」→ 点 **复制到剪贴板**。

生成的 Prompt 结构是「**模板 + 你的实际输入**」，末尾追加的真实内容：

```
---

## 本次输入

{
  "product_name": "HydroJug Traveler 40oz 保温杯",
  "category": "kitchen",
  "core_features": ["40oz 大容量", "手柄便携", "不锈钢真空保温"],
  "target_platform": ["amazon", "independent_site", "tiktok_shop"],
  "target_market": "US",
  "tone": "实用理性"
}
```

> **模板正文里的示例（宠物牵引绳）是给 AI 看的格式范例，不是你的数据** ——
> 你的数据在末尾「本次输入」里。别看到示例就以为填错了（我实跑时先看了开头 600 字符，
> 一度以为参数没传进去）。

## 五层可选

| 层 | 作用 |
|---|---|
| ① 决策层 · 内容策略卡 | 先定卖点排序/关键词/避讳，**不写文案** |
| ② 执行层 · Amazon | 标题 + 五点 + 关键词 |
| ② 执行层 · 独立站 | 长文案 + SEO |
| ② 执行层 · TikTok Shop | 短标题 + 口播稿 |
| ③ 质检层 | 合规风险扫描 |

**正确用法是串起来**：先跑①拿到策略卡 → 把策略卡喂给② → 把②的产出喂给③质检。
直接跳到②会得到「好看但不统一」的文案，三个平台各说一套。

## 为什么不直接在页面里调 AI

**本面板不调用任何 LLM、不需要 API Key、不会把你的数据发给第三方。**
Prompt 生成出来由你自己粘到 Claude / ChatGPT 里跑。

这是刻意的边界：**不碰你的 Key，也不替你产生调用费用。**

---

# ⑧ 变化监控

**Tab →「⑧ 变化监控」** —— 这一步**第一天跑不出东西**，明天才是它的主场。

## 快照与变化对比

| 填什么 | 值 |
|---|---|
| 基准快照 | 昨天那一份 |
| 对比快照 | 今天这一份 |

点 **对比**。

### 真实输出：同类目、同页数（29 行 vs 29 行）

```
告警: （无）
变化条数: 1
类型分布: {'price_drop': 1}
```

```json
{
  "item_id": "B0114BNXQ6",
  "title": "Milk-Bone Flavor Snacks Minis Dog Treats, 36 Oz.",
  "change_type": "price_drop",
  "detail": "价格 11.48 -> 11.36（-1.0%），当前促销：unknown"
}
```

**一天只变 1 条、幅度 1%，这才是真实数据该有的样子。**
如果你的日报一次报出几十条变化，**几乎肯定是对比方式错了** —— 见下面两个坑。

## 坑一：页数不一致 → 凭空多出假「新品上榜」

拿 1 页（30 名）的快照比 2 页（88 名）的快照，真实输出：

```
告警: ⚠ 两份快照的榜单覆盖范围不同（前：第 1-30 名，后：第 1-88 名）。
      范围外的商品会被误判为「新品上榜」，请用相同页数、相同类目的快照对比。
变化条数: 59
类型分布: {'new_listing': 30, 'delisted': 29}
```

59 条**全是假的** —— 不是竞品变了，是我抓的范围变了。
所以第①步要你「记住页数」。

## 坑二：类目不一致 → 更隐蔽，因为行数可能完全相同

实跑时拿 `kitchen`(68 行) 比 `office-products`(68 行)，
**排名范围完全一致**，页数守卫抓不到，却产出 **136 条垃圾变化**（68 假新品 + 68 假下架），
**且不触发任何告警**。

**这是这次实跑才发现的真实缺陷，已经修掉。** 修复后的真实输出：

```
告警: ⚠ 两份快照的类目不同（前：kitchen，后：office-products）。
      不同类目之间没有可比性——前一份的商品会全部被判成「下架」，
      后一份会全部被判成「新品上榜」。请改用同一类目、不同日期的两份快照。
```

修复方式：在 `src/monitor.py` 加了 `snapshot_categories()` 与 `category_warning()`，
并让 `coverage_warning()` **优先报类目问题** —— 它比页数问题更致命，
且无法靠限制排名范围补救。配了 7 个回归测试，现在共 **75 个测试全过**。

> **注意一个副作用**：类目告警优先级更高，所以**两个问题同时存在时只会看到类目那一条**。
> 这是故意的 —— 类目不对时页数是否一致根本不重要。

## 每日工作流

| 填什么 | 值 |
|---|---|
| 输入快照 | 今天抓的那份 CSV |
| 对比的上一个快照日期 | 昨天的快照日期 |

点 **运行工作流**，它把「入库 → 对比 → 生成日报」一次做完，写到 `reports/daily_<日期>.md`。

**✅ 算成功**：日志出现「导入竞品记录 N 条」和「发现 M 处变化」，且 M 是个**小数字**。
M 很大就回头看告警。

---

# 第二天复跑（这才是完整闭环）

第一天只是建立基线。第二天的操作**不一样**，只有 3 步：

| 步骤 | 做什么 | 关键点 |
|---|---|---|
| **①** | 同站点、同类目、**同页数**再抓一次 | 页数必须和昨天一致，否则第⑧步全是假变化 |
| **②** | 看竞品变化日报 | 今天开始有内容了 |
| **⑧** | 基准选昨天、对比选今天 | 先确认**没有告警**，再看变化 |

**③④⑤⑥⑦ 第二天通常不用重跑** —— 它们是「上新」动作，不是「日常」动作。

**只有出现「降价」且涉及你在跟的品时，才需要回到⑥**核算跟不跟价：
把竞品新价填进「实际售价」，看贡献利润率还剩多少、有没有跌破保本价 ¥75.97。

没变化就是没变化 —— **不用为了填满日报去凑动作**。

> 想自动化：第⑧步的「每日工作流」把三件事合成一次点击。
> 再进一步可以挂到系统计划任务，但**抓取失败要有人看到**，别做成静默失败。

---

# 配置：改平台 / 市场 / 站点

**Tab →「配置」** —— 这三类清单**不写在代码里**，都是 `data/*.yaml`，在面板里改完即刻生效。

| 改哪一份 | 文件 | 现有条目 |
|---|---|---|
| 销售平台费率 | `data/platforms.yaml` | **16 个** |
| 目的市场物流 | `data/logistics_rates.yaml` | **10 个** |
| 抓取站点 | `data/sites.yaml` | us/jp/uk/de 可用 |

改完点 **保存并生效**，**不需要重启、不需要动代码**，新平台立刻出现在第⑥步的下拉里。

**写坏了不会有事**：保存前自动备份 → 保存后立刻 YAML 解析并校验必需字段 →
解析失败或缺字段**自动回滚**到修改前版本，并告诉你哪里错了。

## 数据可靠性分级 —— 这个必须知道

| 数据 | 可靠性 |
|---|---|
| 平台**佣金率 / 支付费率** | ✅ 查的公开费率表，**来源链接写在 YAML 文件头注释里** |
| **退货率** `return_rate` | ⚠️ **全是估算占位值**。平台不公布，只能用自己店铺历史订单统计 |
| **物流费率** | ⚠️ **全是估算占位值**。货代一对一议价、不公开挂牌，要 rate card 后替换 |
| **汇率** | ⚠️ 占位量级，**不是实时汇率** |

### ⚠️ 含固定费的平台会被低估成本

eBay 每单 $0.30、Etsy 每单 $0.25 加每 listing $0.20 ——
**本模型只算百分比费率，没有计入固定费**。低客单价商品尤其要注意。

### 一个值得注意的费率发现

**Shopee 跨境比本地贵一倍多**：本地 2–8%，但新加坡跨境约 14%、马来 18.36%，
且 2026-02 起新马泰越**再加 5% 技术服务费**。已按跨境费率单独建了配置项
（`shopee_crossborder_sg` 19%、`shopee_crossborder_my` 23.4%）。

## 加站点不是只改 sites.yaml 就能抓

抓取工具（amzrank）也要支持该站点，否则会报「页面没有榜单元素」。
`fr / it / es / ca / au` 已列在 `sites.yaml` 但 `enabled: false`，
因为**需要先在 `amzrank.py` 的 `SITES` 里补对应配置**。

---

# 常见报错

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 顶部显示「只读演示模式」 | 本地服务没连上 | 看终端报错；或换端口 `python serve.py --port 8920` |
| 端口被占 / 服务起不来 | 上次的进程没退干净 | 见下方命令 |
| 「站点 xx 未启用」 | `sites.yaml` 里 `enabled: false` 或没这个站 | 在「配置」页加条目；**还要确认抓取工具支持** |
| 抓取日志报「页面没有榜单元素」 | 类目 slug 不对，或该站抓取工具不支持 | 去目标站榜单页看地址栏真实 slug |
| 「已入库 0 条」 | slug 存在但榜单页结构变了 | 换个已验证类目，确认工具本身是否还正常 |
| `PermissionError` 读 Excel | Excel 开着该文件，产生 `~$` 锁文件 | 关掉 Excel；工具已会过滤 `~$` 前缀文件 |
| 候选品全被判亏损 | **汇率没填 / 填成 1** | 回第③步填真实汇率 |
| 日报一次报出几十条变化 | 页数或类目不一致 | 看告警；用同类目同页数重抓 |
| 「products.csv 里没有 SKU xx」 | 还没在第③步加入候选品 | 先做第③步 |
| 文案里看到「宠物牵引绳」 | 那是模板给 AI 的格式范例 | 看 Prompt **末尾**的「本次输入」 |
| 定价页自校验报错 | JS 与 Python 公式实现不一致 | 这是真 bug，别忽略 |
| 改了 YAML 但下拉没变 | 保存失败被回滚了 | 看状态栏提示，它会说哪里错 |

清端口：

```powershell
Get-NetTCPConnection -LocalPort 8911 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

杀不掉的僵尸进程直接换端口：

```bash
python serve.py --port 8920
```

---

# 这一遍实跑的真实数据汇总

| 项 | 真实值 |
|---|---|
| 抓取 | us / kitchen / 2 页 → **68 条 / 15 秒** |
| 类目验证 | `kitchen` 首次实测通过（此前已验证 `pet-supplies`/`toys-and-games`/`office-products`） |
| 候选品 | B0CQVWT2NH ¥227.13、B0113UZJE2 ¥78.03（汇率 7.1） |
| 比价 | 3 家报价 → 胜出 ¥29.00，**标价最低的 ¥23.80 到仓 ¥30.35 反而更贵** |
| 评分 | B0CQVWT2NH **83.0**（利润分 20/20）、B0113UZJE2 70.47 |
| 硬淘汰 | TRAVEL-008 贡献利润≤0、TRAVEL-006 合规 banned |
| 利润 | 售价 ¥227.13 → 贡献利润 **¥116.39**、利润率 **51.24%**、保本价 ¥75.97、保本 ROAS 1.95 |
| 定价空间 | 30% 毛利只需 ¥124.47，竞品卖 ¥227 → **¥102 空间** |
| 文案 | 决策层 Prompt **1052 字符** |
| 监控 | 同类目同页数 → **1 条真实降价**（11.48→11.36，-1.0%） |
| 发现并修复 | 跨类目对比不报警的缺陷 → 加 `category_warning` + 7 个测试 |
| 测试 | **75 passed** |

---

# 面试时怎么讲这个项目

按流程讲最清楚，但**每一步都要带上「我做了什么判断」**，不能只说「我点了按钮」：

| 步骤 | 值得讲的点 |
|---|---|
| ① | 页数上限/间隔下限是**服务端强制**的，不是客户端建议 —— 我在乎的是别把对方站点打挂 |
| ③ | **汇率换算**：不换算时三个真实商品全被误判亏损。这是我实际踩到并修掉的 bug |
| ④ | **标价陷阱**：¥23.80 的到仓成本比 ¥29.00 的更贵。归一化口径才能比 |
| ④ | MOQ 不达标的报价**故意不自动剔除** —— 压货换低价是商业判断，工具只算清代价 |
| ⑤ | **硬性淘汰是一票否决不是扣分**。需求 24/25 的品因合规归零 |
| ⑤ | 六项里**只有利润是系统算的** —— 这不是「AI 选品」，是把判断标准结构化 |
| ⑥ | 保本价是**除以** (1−费率)，不是乘。用乘法会系统性低估，越贵的品错得越多 |
| ⑥ | JS 和 Python 两套实现**互相自校验** |
| ⑦ | **不调 LLM、不要 API Key** —— 刻意的边界，不碰用户的 Key |
| ⑧ | **两个覆盖度守卫**，第二个是这次实跑才发现的，配了 7 个回归测试 |
| 配置 | 清单从硬编码移到 YAML，**带备份 + 校验 + 自动回滚** |
| 全局 | **数据可靠性分级**：哪些是查来的、哪些是估算占位值，在代码注释和界面上都标了 |

最该强调的一句：

> **这个工具最大的价值不是自动化，是把「拍脑袋」的地方限制到最小。**
> 六个维度里五个还是人判断的 —— 但利润那一项算错了立刻能被查出来，
> 而且它有一票否决权。

---

*本文档所有数字均为 2026-09-12 实跑输出。抓取数据仅本地分析使用，未纳入公开仓库（见 `.gitignore`）。*
